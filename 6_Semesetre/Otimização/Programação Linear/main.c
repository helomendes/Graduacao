#include <stdio.h>

#define MAX BUFSIZ

int main() {
	char buf[MAX];
	fgets(buf, MAX, stdin);
	
	return 1;

	int n, m;
	//le n e m
	//tem jeitos melhores de alocar vetores e matrizes
	float v[n];
	float q[m][2];
	float c[n][m];
	
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < m; j++) {
			// le c[i][j]
		}
	}

	//encerrada a leitura de dados

	return 0;
}
