#include <stdio.h>

int main() {
	/*
	int x, y = 7;
	x = y + (y % 3);
	if (x/8 < 2)
		x = x + 2;
	printf("1. %d\n", x);
	*/

	/*
	int x = -1,	y = 1;
	if (y == (-x))
		printf("%d\n", x);
	*/

	/*
	int a, b = 0;
	scanf("%d", &a);
	if (a >= 0 && a <= 5)
		b = a;
	else
		b = b;
	*/

	int aux;
	aux = 9/2;
	printf("%d\n", aux/8);

	return 0;
}
